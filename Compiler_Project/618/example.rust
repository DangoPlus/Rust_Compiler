/*
 * Example with Functions
 */

// variables
let mut c;
let mut a = 5;
    
// // function declaration
fn add(a:int, b:int) -> int
{
  // let mut q;
  return a+b;
	// return a;
}
    
// main function
fn main() 
{
  // let mut q;
  c = add(a, 10);
  if (c > 10) {
    print -c;
  }
  else {
    print c;
  }
  println ("Hello World");
}
